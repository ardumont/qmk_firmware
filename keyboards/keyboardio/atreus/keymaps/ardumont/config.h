#pragma once

// retro-tapping: Releasing the key without pressing another will send the original
// keycode even if it is outside the tapping term

// specif per key, especially for KC_A
#define RETRO_TAPPING_PER_KEY
